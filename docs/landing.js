const manifest = window.PROJECT_MANIFEST;const cards = document.getElementById('cards');const metrics = document.getElementById('metrics');cards.innerHTML = manifest.cards.map((card, i)=>`<article class="glass flip" style="--z:${40 + i*20}px"><h3>${card.title}</h3><p>${card.body}</p></article>`).join('');metrics.innerHTML = manifest.kpis.map(item=>`<article class="glass metric"><strong>${item.value}</strong><span>${item.label}</span><p>${item.note}</p></article>`).join('');const scene=document.getElementById('scene');scene.addEventListener('pointermove',(e)=>{const x=(e.clientX/window.innerWidth)-0.5;const y=(e.clientY/window.innerHeight)-0.5;scene.style.setProperty('--rx',`${y*-12}deg`);scene.style.setProperty('--ry',`${x*18}deg`);});